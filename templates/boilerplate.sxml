`(*TOP*
    ,(html->xexp "<!DOCTYPE html>")
    (html (@ (lang "en"))
      (head
        (meta (@ (name "viewport")
                 (content "width=device-width, initial-scale=1.0")))
        (meta (@ (charset "utf-8")))
        (template (@ (target "stylesheets.sxml"))))
    (body
      (insert (@ (expr "content"))))))
