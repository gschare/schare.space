'(*TOP*
    (doctype "html")
    (html (@ (lang "en"))
      (meta (@ (name "viewport")
               (content "width=device-width, initial-scale=1.0")))
      (head
        (meta (@ (charset "utf-8")))
        (template (@ (target "stylesheets.sxml"))))
    (body
      (insert (@ (expr "content"))))))
